# Placeholder character

`Jimmy.glb` is temporary only. It must never be treated as Rrvvfo's final model.
When the user's Rrvvfo model is uploaded, add a proper runtime export pipeline rather than overwriting the master Blender/show asset.

Rrvvfo's binding moved to the supplied faceless DEV model during 3D integration Phase 1. `Jimmy.glb` remains unchanged only as an unrelated archived placeholder and is not loaded for Rrvvfo.
